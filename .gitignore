.DS_Store
.build/
.swiftpm/
*.xcuserdatad
xcuserdata/
DerivedData/

# GeoIP databases are large and separately licensed — fetched via Scripts/fetch-geoip.sh
Resources/geoip/
*.mmdb

# Run transcripts list every host this machine contacted. They are personal data and
# must never be committed.
logs/
*.log

# Tracker data is CC BY-NC-SA 4.0 — fetched via Scripts/fetch-trackers.sh, not redistributed here.
Resources/trackers/
